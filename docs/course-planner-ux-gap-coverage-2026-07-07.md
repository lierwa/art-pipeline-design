# Course Planner UX Gap Coverage - 2026-07-07

## Current Worktree Snapshot

- `git status --short` before this document was created showed existing unrelated changes in `frontend/src/features/canvas/CanvasStage.tsx`, `frontend/src/features/canvasObjectAdapters.ts`, `docs/superpowers/plans/2026-07-07-course-planner-ux-gap-repair.md`, and `scene_library/scene_packs/scene_pack_3cff5bc75d25/chapters/chapter_96eb047029b6/scene_package/{assembly.json,package.json}`.
- `git diff -- docs frontend/src/features/coursePlanner frontend/tests/coursePlanner backend/art_pipeline/course_planner backend/tests/course_planner` produced no diff before this document was created.
- This ledger is docs-only and does not modify Course Planner runtime code, tests, or `scene_library` data.

## G01-G21 Repair Matrix

| Gap | Current state | Owner task | Code evidence | Test evidence | Screenshot evidence | Notes |
|---|---|---|---|---|---|---|
| G01 | verified by Task 7 | Task 2, Task 7 | `SceneCategoryList` keeps the Scene Pack card body as the selection target and exposes edit/archive/delete through one compact `Scene Pack actions` group using shared `course-planner-icon-button` sizing; `coursePlanner.css` keeps that action group nowrap with 30px targets. | Final `npm test -- --run tests/coursePlanner` passed 199 tests, including Scene Pack action density. | `output/verification/board-route-1920x1080.png`. | Scene Pack action labels remain available through aria-label/title while visible text no longer expands the card action area. |
| G02 | verified by Task 7 | Task 2, Task 7 | `SelectedChapterSequence` uses handle/content/actions columns, a named `Chapter actions` group, and an icon-only Designer link with accessible label; `coursePlanner.css` reserves a flexible content column and compact right action column. | Final `npm test -- --run tests/coursePlanner` passed 199 tests, including Chapter row density and delete confirmation assertions. | `output/verification/board-route-1920x1080.png`. | The right action area now contains only compact icon controls; destructive delete still opens confirmation. |
| G03 | verified by Task 7 | Task 1, Task 7 | `ChapterWorkspacePage`, `ChapterAssemblyEditorPage`, `CoursePlannerChrome`, route row CSS keep local header before loading/content branches and expose loading as `role=status`. | `cd frontend && npm test -- --run tests/coursePlanner` passed 199 tests; route loading tests include Chapter and Assembly local context. | `output/verification/chapter-loading-1920x1080.png`; `output/verification/assembly-loading-1920x1080.png`; `output/verification/chapter-route-1920x1080.png`; `output/verification/assembly-route-1920x1080.png`. | Screenshot tool now captures loading and loaded states through the Task 7 visual harness. |
| G04 | verified by Task 7 | Task 1, Task 3, Task 7 | `chapterStudioLayout.css` keeps the Chapter route inside the parent app-shell row, budgets rail/main columns to fit 1280px, and keeps one `CoursePlannerPageHeader` as the local Chapter header/status zone. | Final `npm test -- --run tests/coursePlanner` passed 199 tests, including Chapter route shell and library/final action paths. | `output/verification/chapter-route-1920x1080.png`; `output/verification/chapter-progress-rail.png`; `output/verification/chapter-left-column.png`; `output/verification/chapter-right-column.png`; historical mid-width proof remains in `output/verification/chapter-route-task3-actual-1280x900.png`. | Product top bar, local back action, Chapter heading, and status remain visible; mid-width evidence verifies the route does not horizontally overflow. |
| G05 | repaired by Task 3 | Task 3 | `ChapterSceneStudio` removes duplicated header/status chrome; `ChapterAssemblySummary` no longer duplicates `Saved`; `FinalScenePanel` exposes a confirmed `Replace Final` action; `chapterStudioLayout.css` wraps Complete Image row controls at mid width before the whole Studio stacks. | Same focused Task 3 command passed 21 tests, including drawer reuse, single header/status zone, Complete Image delete confirmation, Empty Scene replacement confirmation, Final replacement confirmation, and 1280px stylesheet contracts. | `output/verification/chapter-route-task3-1920x1080.png` captured via the visual harness for full content layout; actual route evidence is in `output/verification/chapter-route-task3-actual-1920x1080.png` and `output/verification/chapter-route-task3-actual-1280x900.png`. | Visual harness still omits the route page header, so actual-route screenshots are the authoritative header/status and mid-width evidence. |
| G06 | verified by Task 7 | Task 4, Task 5, Task 6, Task 7 | `AssemblyEditorCanvas` removes rejected toolbar noise; `CanvasOverlayLayer`/shared CSS keep placed image content below selected frame/labels and now renders selected groups as one canvas overlay; `CanvasBoxEditLayer` and `CanvasArtboard` keep move/resize/rotation on the shared box-edit path; Task 5/6 repaired Asset Pool and Layer Tree rails. | `cd frontend && npm test -- --run tests/coursePlanner` passed 199 tests; targeted Task 7 reruns passed canvas/layers/properties/screenshot-tool tests after the group overlay fix. | `output/verification/assembly-route-1920x1080.png`; `output/verification/assembly-toolbar.png`; `output/verification/assembly-canvas.png`; `output/verification/assembly-assets.png`; `output/verification/assembly-inspector.png`; `output/verification/assembly-reference-vs-current.png`; `output/verification/assembly-grouping-1920x1080.png`. | Assembly normal, multiselect, grouping, drawer, and delete states now have screenshot evidence. |
| G07 | repaired by Task 5 | Task 5 | `AssemblyAssetPoolPanel` keeps generated-lineage Chapter Assets in a `Generated assets` group before `Uploads`, keeps the import-generated action in the Asset Pool, and moves direct upload into a sticky footer so generated-first grouping does not hide upload. | `cd frontend && npm test -- --run tests/coursePlanner/chapter-workspace-assembly-editor-asset-pool.test.tsx tests/coursePlanner/chapter-workspace-assembly-editor-final.test.tsx tests/coursePlanner/chapter-workspace-assembly-editor-asset-actions.test.tsx tests/coursePlanner/chapter-workspace-assembly-editor-layers.test.tsx` passed 43 tests; backend generated-asset target passed via `cd backend && uv run --python 3.12 --extra dev pytest tests/course_planner/test_scene_package_routes_generated_assets.py -q` with 9 tests. | `output/verification/assembly-asset-pool-after-thumb-fit.png`; `output/verification/assembly-route-reference-compare-after-media-check-1920x1080.png`. | Real-route capture used `http://127.0.0.1:5176/course-planner/chapters/chapter_96eb047029b6/assembly`, not the visual harness fixture. |
| G08 | repaired by Task 5 | Task 5 | `AssemblyAssetPoolPanel` now renders compact asset cards with top thumbnail frame, middle name/metadata/usage, bottom icon actions, group collapse buttons using `course-planner-icon-button`, no duplicate UUID fallback names, no bare `x` dimension text, no hidden actions occupying card width, and no cropped thumbnail content. | Same focused frontend command passed 43 tests, including card structure, no stray `x`, numbered duplicate unnamed assets, usage counts, add/locate actions, collapse `aria-expanded`, sticky footer CSS assertions, and layer/action regressions. | `output/verification/assembly-asset-pool-after-thumb-fit.png`; `output/verification/assembly-route-reference-compare-after-media-check-1920x1080.png`. | Patch hygiene cleanup removed old workspace-level Asset Pool internals and fixed hidden secondary action specificity; real DOM verified panel height equals the left rail, upload footer sits at the bottom, thumbnails fit inside 4:3 frames, action width is 31px, and hidden secondary action width is 0. |
| G09 | verified by Task 7 | Task 4, Task 7 | `AssemblyEditorCanvas` removes the visible `Alignment controls` toolbar group; patch hygiene also removed the stale authoring-canvas alignment control contract, so no remaining runtime alignment surface is claimed. | `chapter-workspace-assembly-editor-canvas.test.tsx` asserts `Alignment controls` and `Align left` are absent; final `npm test -- --run tests/coursePlanner` passed 199 tests. | `output/verification/assembly-toolbar.png`; `output/verification/assembly-route-1920x1080.png`. | No replacement always-visible alignment group was added. |
| G10 | verified by Task 7 | Task 4, Task 7 | Manual save remains as an icon-only `IconButton` with accessible labels (`Save`, `Retry`, `Saving`) and no visible `Save Assembly` text; autosave/statusbar remains the primary save feedback surface. | Canvas, autosave-retry, and layers tests assert no visible `Save Assembly` text/button and no `shared-icon-button-with-label` on save; final `npm test -- --run tests/coursePlanner` passed 199 tests. | `output/verification/assembly-toolbar.png`; `output/verification/assembly-footer.png`. | Retry keeps the existing accessible label expected by autosave tests. |
| G11 | verified by Task 7 | Task 4, Task 7 | `assemblyEditorShell.css`/`assemblyWorkspace.css` remove page padding/card framing around the center editor and keep the Assembly shell/layout/canvas body in remaining-height rows; canvas auto-fit tests remain green. | Final `npm test -- --run tests/coursePlanner` passed 199 tests, including remaining-height canvas body, route sizing, auto-fit after Empty Scene change, and local-only rail width assertions. | `output/verification/assembly-route-1920x1080.png`; `output/verification/assembly-canvas.png`; `output/verification/assembly-reference-vs-current.png`. | Visual QA now covers the normal Assembly state and reference comparison. |
| G12 | repaired by Task 6 and July 8 follow-up | Task 6 | `AssemblyLayerTree` renders compact 38px tree rows with row-level drag and no visible dotted handle, thumbnail/folder, single-line name/role, right visibility eye, and header-only delete; group folder icons use transparent chrome, and group selection is reduced to a left accent line instead of a full blue block. `AssemblyPlacementProperties` now understands group selection; `useAssemblyWorkspaceSaveController` preserves selected group ids across save echoes. | `cd frontend && npm test -- --run tests/coursePlanner/chapter-workspace-assembly-editor-layers.test.tsx tests/coursePlanner/chapter-workspace-assembly-editor-properties.test.tsx tests/coursePlanner/chapter-workspace-assembly-editor-canvas.test.tsx tests/coursePlanner/chapter-workspace-assembly-editor-asset-actions.test.tsx --reporter=dot` passed 63 tests; July 8 targeted rerun passed 20 layer tests. | `output/verification/assembly-task6-layer-tree-group-selected-1920x1080.png`; `output/verification/assembly-task6-layer-tree-multiselect-properties-1920x1080.png`; `output/verification/assembly-grouping-layer-list-current.png`. | Browser DOM verified nowrap name/role, eye action, no row delete buttons, selected group header delete, Properties group summary, no visible dotted drag handle, and compact group/child indentation. |
| G13 | repaired by Task 6 | Task 6 | Layer tree multi-select, group/ungroup, drag reorder, visibility toggles, group-aware save selection, statusbar count, and Properties batch/group projections are covered through the shared layer/canvas selection state. | Same Task 6 command passed 63 tests, including layer-tree modifier multi-select, canvas multi-select/marquee, grouping, group-selection preservation, mixed geometry fields, and batch role/geometry updates. | `output/verification/assembly-task6-layer-tree-group-selected-1920x1080.png`; `output/verification/assembly-task6-layer-tree-multiselect-properties-1920x1080.png`. | Group selection no longer falls to `No selection` / `0 selected`; multi-selection no longer exposes a misleading single-layer delete button in the Layer Tree header. |
| G14 | verified by Task 7 | Task 2, Task 3, Task 5, Task 6, Task 7 | `CompleteSceneImagesPanel`, `EmptySceneImagesPanel`, `FinalScenePanel`, `SceneCategoryList`, `SelectedChapterSequence`, `AssemblyLayerTree` layer/group delete, `AssemblyPlacementProperties` batch remove, and Chapter Asset delete use confirmation before destructive mutations. | Final `npm test -- --run tests/coursePlanner` passed 199 tests, including Board, Chapter media, generated/Chapter Asset, placement, group, and batch delete confirmation paths. | `output/verification/board-route-1920x1080.png`; `output/verification/chapter-route-1920x1080.png`; `output/verification/assembly-delete-dialog-1920x1080.png`. | Assembly group delete explicitly confirms deleting the group and child placements; ungroup remains a separate non-destructive action. |
| G15 | verified by Task 7 | Task 2, Task 3, Task 4, Task 7 | Board actions use compact icon controls; Chapter Studio action density is tightened by removing duplicate header/status chrome; Assembly toolbar uses compact icon controls and icon-only save. | Task 2/3 focused commands passed earlier; final `npm test -- --run tests/coursePlanner` passed 199 tests, including Assembly toolbar contracts. | `output/verification/board-route-1920x1080.png`; `output/verification/chapter-route-1920x1080.png`; `output/verification/assembly-toolbar.png`. | Board, Chapter, and Assembly density now all have visual evidence. |
| G16 | verified by Task 7 | Task 1, Task 7 | `CoursePlannerPageHeader` renders string route statuses through `CoursePlannerStatusBadge role=status`; loading text remains inside `.course-planner-page-header__actions`. | Final `npm test -- --run tests/coursePlanner` passed 199 tests, including Chapter and Assembly loading status-zone assertions. | `output/verification/chapter-loading-1920x1080.png`; `output/verification/assembly-loading-1920x1080.png`; `output/verification/assembly-route-1920x1080.png`. | Route status now has stable header action zone and accessible status semantics across loading and loaded states. |
| G17 | verified by Task 7 | Task 2, Task 3, Task 7 | Board density repairs remain in `coursePlanner.css`; Chapter Studio avoids duplicate title/status rows and keeps the 1280px rail/main/media-row width budget inside the viewport. | Final `npm test -- --run tests/coursePlanner` passed 199 tests. | `output/verification/board-route-1920x1080.png`; `output/verification/chapter-route-1920x1080.png`; historical mid-width proof remains in `output/verification/chapter-route-task3-actual-1280x900.png`. | Board and Chapter Studio density are verified; Assembly workspace density is covered separately by G06/G11/G15. |
| G18 | verified by Task 7 | Task 5, Task 7 | `GeneratedChapterAssetsDrawer` opens from the Asset Pool import action through `CoursePlannerDrawer` with `overlay`; it is not nested inside `.assembly-editor-layout`. | `chapter-workspace-assembly-editor-asset-pool.test.tsx` asserts the drawer role, generated output states, disabled unavailable/added actions, and `drawer.closest(".assembly-editor-layout") === null`; final `npm test -- --run tests/coursePlanner` passed 199 tests. | `output/verification/assembly-drawer-overlay-1920x1080.png`; `output/verification/assembly-drawer-overlay.png`; `output/verification/assembly-drawer-reference-vs-current.png`. | Drawer overlay evidence is now generated by the Task 7 screenshot harness. |
| G19 | verified by Task 7 | Task 3, Task 5, Task 7 | Generated assets and Chapter library drawers use the shared overlay drawer path rather than editor-grid children. | Final `npm test -- --run tests/coursePlanner` passed 199 tests; drawer overlay assertions are in the Asset Pool and library-action specs. | `output/verification/assembly-drawer-overlay-1920x1080.png`; `output/verification/assembly-drawer-reference-vs-current.png`. | Visual drawer overlay evidence is now present. |
| G20 | verified by Task 7 | Task 0, Task 7 | Task 4 adds explicit toolbar, z-order/cursor, rotation, and framing assertions; Task 6 adds compact layer-tree/group-selection tests; Task 7 stabilized the screenshot harness and added group-level canvas selection overlays. | `cd frontend && npm test -- --run tests/coursePlanner` passed 199 tests; `cd frontend && npm run build` passed; `cd backend && uv run --python 3.12 --extra dev pytest tests/course_planner -q` passed 186 tests; `git diff --check` passed. | `output/verification/board-route-1920x1080.png`; `output/verification/chapter-loading-1920x1080.png`; `output/verification/chapter-route-1920x1080.png`; `output/verification/assembly-loading-1920x1080.png`; `output/verification/assembly-route-1920x1080.png`; `output/verification/assembly-drawer-overlay-1920x1080.png`; `output/verification/assembly-multiselect-1920x1080.png`; `output/verification/assembly-grouping-1920x1080.png`; `output/verification/assembly-delete-dialog-1920x1080.png`; plus reference comparisons. | Final visual QA now covers Board, Chapter, Assembly, drawer, multi-select, grouping, loading, and delete dialog states. |
| G21 | verified by Task 7 | Task 0, Task 7 | This ledger now maps G01-G21 and July 6 Issue 1-42 to explicit code/test/screenshot evidence or scoped notes. | Final frontend, backend, build, screenshot, and hygiene commands are recorded in the Verification Ledger below. | Task 7 screenshot set and comparison images listed in G20. | No July 6 row remains unverified. |

## July 6 Issue Coverage Matrix

| ID | Decision summary | Current state | Owner task | Evidence | Notes |
|---:|---|---|---|---|---|
| 1 | Keep product TopAppBar on Board, Chapter, Assembly; add local back/breadcrumbs | verified by Task 7 | Task 1, Task 7 | `course-planner-routing.test.tsx` and route workspace tests verify product TopAppBar plus local Chapter/Assembly context; screenshots: `board-route-1920x1080.png`, `chapter-loading-1920x1080.png`, `chapter-route-1920x1080.png`, `assembly-loading-1920x1080.png`, `assembly-route-1920x1080.png`. | TopAppBar and local route chrome remain visible on Board, loading, and loaded deep routes. |
| 2 | Chapter library actions use drawer/sheet, not inline disclosure | repaired by Task 3 | Task 3 | `cd frontend && npm test -- --run tests/coursePlanner/chapter-workspace.test.tsx tests/coursePlanner/chapter-workspace-library-actions.test.tsx tests/coursePlanner/chapter-workspace-assembly-editor-final.test.tsx` passed 19 tests; `chapter-workspace-library-actions.test.tsx` verifies Bind Character IP and Upload Reference open `CoursePlannerDrawer` and no inline `details` exists. | Screenshot evidence: `output/verification/chapter-route-task3-actual-1920x1080.png`; G19 still tracks later generated-assets drawer audit for Task 5. |
| 3 | Assembly reuses/extracts main art-pipeline selection/tree protocols | verified by Task 7 | Task 4, Task 6, Task 7 | Shared canvas move/resize/rotate flows use `CanvasArtboard` and `CanvasBoxEditLayer`; Layer Tree grouping/reorder uses the shared selection/move protocol; final `npm test -- --run tests/coursePlanner` passed 199 tests. | Task 7 added group-level canvas selection projection so right tree group selection and canvas selection now agree. |
| 4 | Restore grouping as first-class Layer Tree operation | repaired by Task 6 | Task 6 | Task 6 focused command passed 63 tests; browser proof: `output/verification/assembly-task6-layer-tree-group-selected-1920x1080.png`. | Group rows, child indentation, group/ungroup, selected group Properties, and group delete confirmation are now verified. |
| 5 | Asset Pool primary source is pipeline outputs materialized into chapter assets | repaired by Task 5 | Task 5 | Focused frontend command passed 43 tests proving `Generated assets` appears before `Uploads` when generated-lineage assets exist; backend generated-asset target passed via `uv run --python 3.12 --extra dev pytest tests/course_planner/test_scene_package_routes_generated_assets.py -q` with 9 tests. | Real-route Asset Pool evidence: `output/verification/assembly-asset-pool-after-thumb-fit.png`. |
| 6 | Scene Pack Board is a dense planning workspace | verified by Task 7 | Task 2, Task 7 | `scene-category-board.test.tsx` and `scene-category-board-chapter-status.test.tsx` verify dense row/action structure; screenshot: `output/verification/board-route-1920x1080.png`. | G01/G02/G17 cover Board density and action composition. |
| 7 | Chapter Studio keeps structure but gets local optimization | repaired by Task 3 | Task 3 | Same focused Task 3 command passed 19 tests; `ChapterSceneStudio` keeps the existing panel structure while removing duplicate Chapter header/status chrome and keeping one page-header status zone. | Screenshot evidence: `output/verification/chapter-route-task3-actual-1920x1080.png` and `output/verification/chapter-header-status-task3-actual-1920x220.png`. |
| 8 | Assembly keeps three-column editor with corrected side rails | verified by Task 7 | Task 4, Task 5, Task 6, Task 7 | Task 4 preserves the resizable three-panel editor, Task 5 repairs Asset Pool, Task 6 repairs Layer Tree/Properties, and final `npm test -- --run tests/coursePlanner` passed 199 tests. | Screenshot evidence: `assembly-route-1920x1080.png`, `assembly-assets.png`, `assembly-canvas.png`, `assembly-inspector.png`. |
| 9 | Delete or rewrite old tests/patches protecting rejected behavior | verified by Task 7 | Task 0, Task 4, Task 5, Task 6 | Patch Hygiene Rejected Locks below maps each rejected behavior to delete/rewrite disposition; Task 4/5/6 tests invert the visible `Save Assembly`, always-visible alignment, dashed show-boxes, old Asset Pool card, 64px layer row, single-selection-only, Uploads-dominant, single-placement, and delete-without-confirmation locks. | No compatibility branch was kept for known-rejected behavior. |
| 10 | ADR 0018 remains architecture reference | verified by Task 7 | Task 0, Task 7 | ADR 0018 remains the protocol architecture source; Task 4/6 implementation routes shared canvas/layer selection through existing protocol boundaries, and final tests/build passed. | This row is an architecture-reference constraint, not a user-facing screenshot item. |
| 11 | Protocol cleanup precedes visual-only work | verified by Task 7 | Task 0, Task 4, Task 6, Task 7 | Task 4 deleted tests/code that protected visible `Save Assembly`, always-visible alignment controls, and top-toolbar `Show boxes`; Task 6 repaired layer/group protocol; Task 7 added group canvas projection backed by tests. | Visual screenshots now sit on the cleaned protocol path rather than a second fake selection system. |
| 12 | Pipeline asset picker shows current Chapter Complete Image runs first | verified by Task 5 tests | Task 5 | Backend generated-asset tests passed 9 tests via `uv run --python 3.12 --extra dev pytest tests/course_planner/test_scene_package_routes_generated_assets.py -q`; they cover current Chapter Complete Image run assets and exclusion of another Chapter's run assets. | The requested plain `python -m pytest ...` command cannot start in this shell because `python` is not on PATH. |
| 13 | Selected pipeline run assets copy into chapter-owned assets | verified by Task 5 tests | Task 5 | Backend test `test_materialize_generated_asset_copies_into_chapter_assets` passed and verifies copied PNG bytes, ChapterAsset lineage, and returned `chapterAsset`; frontend drawer test verifies the import action surface. | No production-art quality gate was introduced. |
| 14 | Default dedupe by source run asset per Chapter | verified by Task 5 tests | Task 5 | Backend idempotency tests passed for same run asset and across Complete Images, returning the existing ChapterAsset id instead of duplicating Chapter Assets. | Dedupe remains Chapter-local through stored generated lineage. |
| 15 | Chapter Asset deletion affects current Chapter/Assembly refs only | verified by Task 7 | Task 5, Task 6, Task 7 | Backend test `test_delete_materialized_generated_asset_preserves_source_outputs` verifies source outputs remain while current Chapter Asset and Assembly refs are cleared; Task 6 delete tests cover placement/group destructive paths with confirmation. | Screenshots include `assembly-delete-dialog-1920x1080.png`; generated source deletion and layer deletion are separate confirmed boundaries. |
| 16 | Assembly canvas fills remaining viewport, not fixed height | verified by Task 7 | Task 4, Task 7 | Final `npm test -- --run tests/coursePlanner` passed 199 tests; route/canvas tests verify `height: 100%`, `min-height: 0`, no `100vh`, and non-pixel canvas body sizing. | Visual proof: `assembly-route-1920x1080.png` and `assembly-canvas.png`. |
| 17 | Resized panel widths are local only, not manifest | verified for Task 4 | Task 4, Task 7 | Expanded Task 4 command passed 77 tests; navigation shell test saves after a local panel layout write and asserts saved manifest JSON does not contain panel/rail/layout state. | Task 7 can re-audit final manifest boundaries. |
| 18 | Canvas auto-fit before user manipulation and after Empty Scene change | verified for Task 4 | Task 4 | Expanded Task 4 command passed 77 tests; canvas test verifies zoom is reset by Empty Scene identity/size change after user zoom. | Replacement transform preservation remains separate Task 6 scope. |
| 19 | Canvas operations follow mature editor conventions | verified by Task 7 | Task 4, Task 6, Task 7 | Final `npm test -- --run tests/coursePlanner` passed 199 tests covering shared move/resize, selected frame/handles, rotation handle, marquee, multi-select, select-all, nudge, delete confirmation, undo/redo, save, pan, zoom, fit, context menu, grouping, and layer-tree selection. | Visual proof: `assembly-multiselect-1920x1080.png`, `assembly-grouping-1920x1080.png`, `assembly-delete-dialog-1920x1080.png`. |
| 20 | Layer Tree follows mature layer-panel conventions | repaired by Task 6 and July 8 follow-up | Task 6 | Task 6 focused command passed 63 tests; July 8 focused rerun passed 20 layer tests. Browser evidence now verifies compact 38px rows, no row delete, no visible dotted drag affordance, row-level drag preservation, thumbnails/transparent folder, role column, eye actions, compact parent/child indentation, and reduced selected group state; screenshots: `output/verification/assembly-task6-layer-tree-group-selected-1920x1080.png` and `output/verification/assembly-grouping-layer-list-current.png`. | Lock controls remain intentionally omitted in this pass; the right-side icon area is reserved by the eye column. |
| 21 | Placement and Group names editable without renaming source asset | verified by Task 6 | Task 6 | Properties tests verify raw placement names are saved exactly while source Asset Pool names remain unchanged; Task 6 browser proof shows group name editable in Properties without changing source asset cards. | Placement/group labels are Assembly-local facts, initialized from assets but not kept in sync with source asset display names. |
| 22 | Autosave persists manifest facts only | verified by Task 7 | Task 1, Task 4, Task 7 | Final `npm test -- --run tests/coursePlanner` passed 199 tests; autosave/autosave-retry tests cover transform save echoes/conflicts/retry, and rail width/group collapse tests confirm UI layout/collapse state is excluded from saved manifest. | Status proof appears in Assembly screenshots and `assembly-footer.png`. |
| 23 | Layer Tree collapsed state stays local, not manifest | verified by Task 6 | Task 6 | `chapter-workspace-assembly-editor-layers.test.tsx` verifies collapsing a group does not call the save manifest path after autosave debounce. | Collapse remains editor-local UI state, not manifest state. |
| 24 | Assembly includes baseline mature-editor shortcuts | verified by Task 7 | Task 4, Task 6, Task 7 | Final `npm test -- --run tests/coursePlanner` passed 199 tests covering undo, redo, save, temporary pan, tool switching, zoom, select-all, group, ungroup, nudge, confirmed Delete, and Escape menu close. | Keyboard and layer/canvas selection shortcuts now share the same selection model. |
| 25 | Every delete operation requires confirmation | verified by Task 7 | Task 2, Task 3, Task 5, Task 6, Task 7 | Board, Chapter media, Final replacement, generated-asset/Chapter Asset, Assembly placement, group, and batch remove confirmation tests all pass in final `npm test -- --run tests/coursePlanner`; screenshot: `assembly-delete-dialog-1920x1080.png`. | Destructive operations use `ConfirmActionDialog` or the existing alert-dialog confirmation boundary. |
| 26 | Deleting group deletes group plus child placements; ungroup separate | verified by Task 6 | Task 6 | Task 6 focused command passed group delete confirmation and ungroup preservation tests; browser proof: `output/verification/assembly-task6-group-delete-dialog-1920x1080.png`. | Delete confirms destructive removal; ungroup keeps child placements and is a separate header action. |
| 27 | Undo/redo covers only current Assembly manifest edits | verified for Task 4 | Task 4 | Expanded Task 4 command passed 77 tests; canvas tests verify undo/redo changes manifest facts without restoring stale selection state. | Final Task 7 can re-audit whole-workflow history boundaries. |
| 28 | Properties rail supports batch editing for multi-selection | verified by Task 6 | Task 6 | `chapter-workspace-assembly-editor-properties.test.tsx` verifies batch role edits, mixed geometry placeholders, shared pixel value application, and confirmed batch removal; screenshot: `output/verification/assembly-task6-layer-tree-multiselect-properties-1920x1080.png`. | Group-only selection now shows editable group name plus child-count summary instead of an empty Placement panel. |
| 29 | Layer Tree top row represents front/topmost canvas layer | verified by Task 6 | Task 6 | `chapter-workspace-assembly-editor-layers.test.tsx` verifies front-to-back labels, root reorder projection, and canvas overlay z-order sync after Arborist move. | Tree order and canvas overlay order share the same manifest `layer_order` projection. |
| 30 | New placements use display size separate from raw source dimensions | verified by Task 7 | Task 4, Task 5, Task 7 | Canvas tests verify drop/click-add/generated-square placements use the 25 percent initial display policy rather than raw asset pixels; final `npm test -- --run tests/coursePlanner` passed 199 tests. | Generated materialized assets enter the same Chapter Asset placement path. |
| 31 | Same Chapter Asset can be placed multiple times | verified by Task 5 tests | Task 5, Task 6 | Focused frontend command passed; Asset Pool test adds another placement for a used asset while keeping Locate available and updates usage count to `2 uses`. | Task 6 may still extend this through layer/group interactions. |
| 32 | Repeated placements get distinct default names | verified by Task 5 tests | Task 5, Task 6 | Focused frontend command passed; Asset Pool test verifies the second placed Breakfast bowl becomes `Breakfast bowl 2`. | Names remain assembly-local placement labels. |
| 33 | Drag Asset Pool item to canvas creates placement | verified by Task 7 | Task 4, Task 5, Task 7 | `chapter-workspace-assembly-editor-canvas.test.tsx` covers Asset Pool drag/drop into the canvas and placement creation; final `npm test -- --run tests/coursePlanner` passed 199 tests. | Drag/drop uses the same placement creation policy as click-add. |
| 34 | Pipeline-derived assets are primary Asset Pool grouping | repaired by Task 5 | Task 5 | Focused frontend command passed; generated-lineage assets render in `Generated assets` before `Uploads`, with Uploads secondary and direct upload in a sticky footer. | Real-route proof: `output/verification/assembly-asset-pool-after-thumb-fit.png`; full route proof: `output/verification/assembly-route-reference-compare-after-media-check-1920x1080.png`. |
| 35 | Generated asset import workflow lives in Assembly Asset Pool | verified by Task 7 | Task 5, Task 7 | Asset Pool tests open `Generated Chapter Assets` from the Asset Pool `Import generated assets` button and assert the drawer is outside `.assembly-editor-layout`; final `npm test -- --run tests/coursePlanner` passed 199 tests. | Screenshot proof: `assembly-drawer-overlay-1920x1080.png` and `assembly-drawer-reference-vs-current.png`. |
| 36 | Completed Codex-generated image files import regardless quality | verified by Task 5 tests | Task 5 | Backend test `test_materialize_generated_asset_ignores_quality_report_failures` passed; listing and materialization ignore failed quality reports when a Codex image file exists and is readable. | Functional test assets are accepted regardless of production realism. |
| 37 | Unavailable generated outputs show disabled state and reason | verified by Task 5 tests | Task 5 | Backend listing test marks missing/unreadable files unavailable with reasons; frontend drawer test shows the unavailable reason and disables the add action. | No silent failure path is claimed. |
| 38 | No nested groups in first version | verified by Task 6 | Task 6 | `AssemblyLayerTree` only permits dropping placement nodes into group parents and `assemblyLayerTreeModel` keeps one-level group ids; Task 6 focused command passed 63 tests across grouping and reordering. | This is a first-version single-level group tree, not a nested group editor. |
| 39 | Basic alignment/nudging is scoped; no complex snapping | repaired for Task 4 toolbar scope | Task 4 | Expanded Task 4 command passed 77 tests; toolbar tests verify always-visible alignment controls are absent while nudge shortcuts still update manifest transforms. | No complex snapping or replacement alignment UI was introduced. |
| 40 | Canvas marquee selection included in first version | verified by Task 7 | Task 6, Task 7 | `chapter-workspace-assembly-editor-canvas.test.tsx` verifies marquee selection integrates with shared selected ids, properties batch state, and layer tree selection; final `npm test -- --run tests/coursePlanner` passed 199 tests. | Marquee feeds the same `selectedLayerNodeIds`/placement selection flow used by Layer Tree and Properties. |
| 41 | Empty Scene replacement confirms and preserves relative transforms | verified by Task 7 | Task 3, Task 6, Task 7 | `chapter-workspace-library-actions.test.tsx` verifies confirmation before Empty Scene replacement; `chapter-workspace-assembly-editor-properties.test.tsx` now asserts explicit save after replacement keeps `placement_bowl` and `placement_cloth` relative `transform` values unchanged while updating `empty_scene_image_id` and `empty_scene_size`. | Replacement enters alignment review and blocks autosave until explicit author save or clear. |
| 42 | Assembly readiness/final lock uses manifest/domain facts only | verified by Task 7 | Task 1, Task 3, Task 7 | Final `npm test -- --run tests/coursePlanner` passed 199 tests; `chapter-workspace-assembly-editor-final.test.tsx` verifies readiness from manifest/domain facts, final lock remains available through transient UI state changes, and replacing a locked Final confirms before overwriting authored snapshots. | Screenshot evidence: `output/verification/chapter-route-1920x1080.png`; Assembly editor visual evidence is covered by G20. |

## Patch Hygiene Rejected Locks

These existing or potential old-patch behaviors are rejected. Owning tasks must delete or rewrite tests, code, CSS, helpers, comments, and names that protect them rather than layering compatibility branches on top.

| Rejected lock | Owning task | Required disposition |
|---|---|---|
| Tests or code protecting hidden `TopAppBar` | Task 1 | Rewrite to require product `TopAppBar` plus local back/breadcrumbs on deep routes. |
| Tests or code protecting visible text `Save Assembly` in the canvas toolbar | Task 4 | Replace with icon-only save affordance or autosave/status evidence. |
| Tests or code protecting always-visible alignment toolbar | Task 4 | Delete or re-scope into an accepted contextual/menu/shortcut surface. |
| Tests or code protecting the top-toolbar `Show boxes` dashed-square toggle | Task 4 | Remove the duplicate visible box system and test selected-frame behavior instead. |
| Tests or CSS protecting current long/tall Asset Pool card image layout | Task 5 | Rewrite around compact card structure with fixed rectangular image frame and sticky upload. |
| Tests or CSS protecting `64px` layer rows that force layer names and roles onto separate lines | Task 6 | Rewrite around compact single-row tree-aware layer rows. |
| Tests or code proving only single selection | Task 4, Task 6 | Replace with shared multi-select, marquee, layer range/toggle, and batch edit coverage. |
| Tests or code making `Uploads` the dominant asset pool state when generated assets exist | Task 5 | Rewrite around generated pipeline assets as the primary group and Uploads as secondary. |
| Tests or code relying on one asset creating only one placement | Task 5, Task 6 | Replace with multiple placements per Chapter Asset and usage-count assertions. |
| Tests or code proving delete without confirmation | Task 3, Task 5, Task 6 | Replace with `ConfirmActionDialog`/existing confirmation-path tests for every destructive action. |

## 2026-07-08 Follow-up Coverage

| Gap / issue | Added evidence | Notes |
|---|---|---|
| G06, G20, Issue 19, Issue 40 | `AssemblyAuthoringCanvas` now portals the live marquee into the transformed `canvas-artboard`; `chapter-workspace-assembly-editor-canvas.test.tsx` verifies both scaled source-coordinate selection and live marquee DOM placement/style after a scaled, offset drag. | This covers the user-reported regression where the mouse stayed inside the artboard but the visual selection rectangle rendered in the outer workspace after zoom. |
| G13, Issue 20, Issue 29 | `chapter-workspace-assembly-editor-layers.test.tsx` now exercises a real UI path: add an unused Asset Pool item, drag the resulting root placement into an existing group through the Arborist move hook, then assert tree depth, canvas z-order, saved `groups[].placement_ids`, placement `group_id`, and `layer_order`. | This closes the previous gap where root-to-group drag was mostly protected at model/protocol level, not the live layer tree path. |
| G12, Issue 20 | `AssemblyLayerTree` and `assemblyRightRail.css` now remove the visible dotted drag affordance, keep the whole row as the Arborist drag handle, collapse the extra placement spacer, use 38px rows, transparent group folder icons, and a left-accent-only group selection state. | This addresses the user-marked sparse right-rail layer list, the unwanted drag-dot column, and the heavy group selected/folder chrome while preserving drag/group behavior. |
| G20, G21 | `capture-course-planner-screenshots.mjs` now includes a real headless-Chrome scaled-marquee probe and writes `output/verification/assembly-marquee-scaled-drag-1920x1080.png`. | The probe clicks zoom out twice, drags from blank artboard space, and compares the browser `getBoundingClientRect()` for `.assembly-marquee` with the actual mouse start/end points. |

## Verification Ledger

- 2026-07-08 follow-up frontend: `cd frontend && npm test -- --run tests/coursePlanner` passed 24 files / 202 tests.
- 2026-07-08 follow-up build: `cd frontend && npm run build` passed; Vite reported only the existing chunk-size warning.
- 2026-07-08 follow-up browser probe: `cd frontend && node tools/capture-course-planner-screenshots.mjs --marquee-probe-only` passed and captured `output/verification/assembly-marquee-scaled-drag-1920x1080.png`.
- 2026-07-08 follow-up screenshots: `cd frontend && node tools/capture-course-planner-screenshots.mjs` passed, including `verified scaled marquee`, normal reference comparisons, crops, and the new scaled-marquee screenshot.
- 2026-07-08 layer-list density follow-up: `cd frontend && npm test -- --run tests/coursePlanner/chapter-workspace-assembly-editor-layers.test.tsx` passed 20 tests; targeted Chrome/CDP capture refreshed `output/verification/assembly-grouping-layer-list-current.png`.
- 2026-07-08 follow-up hygiene: `git diff --check` passed with no whitespace errors.
- Task 7 final frontend: `cd frontend && npm test -- --run tests/coursePlanner` passed 24 files / 199 tests.
- Task 7 final build: `cd frontend && npm run build` passed; Vite reported only the existing chunk-size warning.
- Task 7 final backend: `cd backend && uv run --python 3.12 --extra dev pytest tests/course_planner -q` passed 186 tests with one Starlette/httpx deprecation warning. The literal `python -m pytest ...` command is not available in this shell because `python` is not on PATH.
- Task 7 final screenshots: `cd frontend && node tools/capture-course-planner-screenshots.mjs` passed and captured Board, Chapter loading/loaded, Assembly loading/normal/drawer/multiselect/grouping/delete states plus crops and reference comparisons.
- Task 7 final coverage audit: `missing=0; for i in {1..42}; do if ! rg -q "^\| ${i} \|" docs/course-planner-ux-gap-coverage-2026-07-07.md; then echo "missing issue row ${i}"; missing=1; fi; done; for g in $(seq -w 1 21); do if ! rg -q "^\| G${g} \|" docs/course-planner-ux-gap-coverage-2026-07-07.md; then echo "missing gap row G${g}"; missing=1; fi; done; if [ "$missing" -eq 0 ]; then echo "all G01-G21 and issue rows 1-42 present"; fi; exit "$missing"` passed.
- Task 7 final state audit: `if rg -n "\| [^|]*\| (broken|unverified|pending|partial|partially|not re-verified|functionally verified|screenshot pending)" docs/course-planner-ux-gap-coverage-2026-07-07.md; then exit 1; else echo "no unresolved row states in coverage ledger"; fi` passed.
- Task 7 doc whitespace audit: `if rg -n "[[:blank:]]$" docs/course-planner-ux-gap-coverage-2026-07-07.md docs/superpowers/plans/2026-07-07-course-planner-ux-gap-repair.md; then exit 1; else echo "no trailing whitespace in task docs"; fi` passed.
- Task 7 final hygiene: `git diff --check` passed with no whitespace errors.
- `rg -n "2026-07-07-course-planner-ux-gap-repair|G0|G1|G2|\| 42 \|" docs/course-planner-ux-gap-coverage-2026-07-07.md` passed and matched the pre-existing plan file snapshot, G01-G21 matrix, and Issue 42 row.
- `missing=0; for i in {1..42}; do if ! rg -q "^\| ${i} \|" docs/course-planner-ux-gap-coverage-2026-07-07.md; then echo "missing issue row ${i}"; missing=1; fi; done; if [ "$missing" -eq 0 ]; then echo "all issue rows 1-42 present"; fi; exit "$missing"` passed with `all issue rows 1-42 present`.
- `if rg -n "[[:blank:]]$" docs/course-planner-ux-gap-coverage-2026-07-07.md; then exit 1; else echo "no trailing whitespace in coverage ledger"; fi` passed with `no trailing whitespace in coverage ledger`.
- `git diff --check` passed with no tracked-diff whitespace errors. This does not validate untracked files; the coverage ledger is covered by the explicit trailing-whitespace check above.
- No commit requested; no commit should be made for Task 0.
- Task 5 RED: requested backend command `cd backend && python -m pytest tests/course_planner/test_scene_package_routes_generated_assets.py -q` could not start because `python` is not on PATH; `cd backend && uv run --python 3.12 --extra dev pytest tests/course_planner/test_scene_package_routes_generated_assets.py -q` passed 9 tests, showing backend behavior was already green after the new quality-report independence test. Requested frontend command failed with 2 expected Asset Pool failures: missing `Collapse Generated assets` button and missing `.assembly-asset-card-top` card structure.
- Task 5 GREEN: `cd backend && uv run --python 3.12 --extra dev pytest tests/course_planner/test_scene_package_routes_generated_assets.py -q` passed 9 tests with one Starlette/httpx deprecation warning; `cd frontend && npm test -- --run tests/coursePlanner/chapter-workspace-assembly-editor-asset-pool.test.tsx tests/coursePlanner/chapter-workspace-assembly-editor-final.test.tsx` passed 21 tests, with an npm update-check warning after exit 0.
- Task 5 harness screenshot failure is superseded by Task 7: `cd frontend && node tools/capture-course-planner-screenshots.mjs` now passes and captures Board, Chapter loading/loaded, Assembly loading/normal/drawer/multiselect/grouping/delete states plus crops and reference comparisons.
- Task 5 real-route correction after local screenshot mismatch: `cd frontend && npm test -- --run tests/coursePlanner/chapter-workspace-assembly-editor-asset-pool.test.tsx tests/coursePlanner/chapter-workspace-assembly-editor-final.test.tsx tests/coursePlanner/chapter-workspace-assembly-editor-asset-actions.test.tsx tests/coursePlanner/chapter-workspace-assembly-editor-layers.test.tsx` passed 43 tests; `git diff --check` passed. Direct Playwright capture against `http://127.0.0.1:5176/course-planner/chapters/chapter_96eb047029b6/assembly` produced `output/verification/assembly-asset-pool-after-thumb-fit.png` and `output/verification/assembly-route-reference-compare-after-media-check-1920x1080.png` after comparing against `docs/assets/course-planner-assembly-editor-normal-reference.png`.
